# 前端 API 執行模式（複製成 .env 或 .env.local 後修改）
#
# mock（預設）：完全在前端跑，回應形狀照契約 v2。無需後端即可 demo。
# real         ：直接打真後端的 24 支 REST + SSE chat（docs/handoff/2026-09-12-frontend-contract-v2.md）。
#
# 接上後端後，只要把下面改成 real 就好，其餘程式碼一行都不用動。
VITE_API_MODE=mock

# REST／SSE 路徑前綴。預設 /api，vite dev proxy 已代理到 http://localhost:8091（見 vite.config.js）。
# 若後端在別的位址，改這裡（例如正式環境的完整網址）。
VITE_API_BASE=/api
