"""卷宗層：一案一份 `manifest.json`（書籤清單）＋ 母庫查（Bedrock KB／S3）。

**這一層不是資料庫，也不是搜尋索引。** 兩件事要分清楚（契約 v2 §4.0）：

- **母庫 ＝ Bedrock KB ＋ S3**，那才是「資料庫」，而且已經有了。**搜尋打那裡。**
- **`manifest.json` ＝書籤清單**，只存「這個案子挑了哪些東西」與顯示欄位快取。
  **搜尋完全不經過它。** 所以「不加 DB」跟「能不能搜尋」是兩件無關的事。
"""
