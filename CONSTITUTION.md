# CONSTITUTION — 訴願 AI Prototype（新北黑客松 2026）

> sprint 模式的憲法：條文少但每條都是紅線。AI agents 與人都遵守。
> 技術選型見 `docs/tech-stack-decision.md`；計畫文件見 `plans/`。

## 原則

### 1. 分層誠實（產品核心主張，也是工程紀律）
系統輸出分三級並明確標示：**可驗算的**（規則引擎，攤開算式與法條依據）、
**有出處的**（RAG，每句標判解/法條字號）、**請人工判斷的**（拒絕生成，只給風險提示）。
任何功能若無法歸入三級之一，不得進 demo。

### 2. 引用必可驗
草稿或建議中出現的每一個法條、判解字號、函釋，必須能對應到資料集內實際存在的檔案
或明確標示「庫外，未驗證」。**生成一個查無此號的判例＝P0 bug**，比 crash 嚴重。

### 3. 不編造測資背景
訴願書樣本賽方沒給。自造測資必須：(a) 從真實決定書的事實欄反推、(b) 檔名標明 `synthetic-`、
(c) demo 時口頭聲明是合成資料。不得暗示為真實案件。

### 4. 規則引擎零 LLM 依賴
期間計算（訴願法 14/15/16/17＋民法）是純函式：同輸入必同輸出、可單元測試、
測試集=資料集裡的歷史案。LLM 只負責上游抽取，不碰計算。

### 5. Plan 先行，帶驗證條款
每個工作包開工前在 `plans/` 有一份 plan（目標/步驟/**驗收條件**/回滾方式）。
驗收條件必須是可執行的檢查（跑什麼、看到什麼），不是「應該可以」。
完成宣稱需附驗收證據（輸出/截圖/測試結果）。

### 6. Demo 資產隔離
賽方資料集僅供競賽之用：不進 git、S3 bucket 不公開、demo 影片與簡報中的案件一律用
已去識別化的決定書原文或合成測資。

### 7. 雲與 secret 隔離（團隊核心規則 1 的本地化）
本專案只碰 AWS。不借用其他專案的 GCP 的任何憑證/服務帳號。secret 只在 `.env`（gitignored）
與 AWS 自身的 secret 管理。明文 key 進 git＝事故，不分分支。

### 8. 30 小時紀律
砍功能優先於加功能。每個 Phase 有「最遲放棄時刻」：到點沒綠就降級到備援方案，
不戀戰。備援方案在 plan 裡事先寫好，不臨場發明。

## 約束

- [ ] 不得在 request 路徑放同步阻塞呼叫（FastAPI async 慣例，沿用 既有專案）
- [ ] 前端不得直連 Bedrock（一律過後端，key 不落瀏覽器）
- [ ] 每個 PR/commit 訊息寫「做了什麼」，不寫「更新檔案」
- [ ] backlog story 一律使用者價值句，技術任務留在 plans/
